<?php
namespace {{ namespace }}\Controller;

use PSFS\base\types\{{ controllerType }}Controller;

/**
* Class {{ class }}Controller
* @package {{ namespace }}\Controller
* @author {{ get_config("author", "Fran López") }} <{{ get_config("author_email", "fran.lopez84@hotmail.es") }}>
* @version {{ get_config("version", "1.0") }}
* @Api {{ namespace }}
* Autogenerated controller [{{ ""|date("Y-m-d H:i:s") }}]
*/
class {{ class }}Controller extends {{ controllerType }}Controller {
    const DOMAIN = '{{ class }}';

    /**
    * @Inyectable
    * @var \{{ namespace }}\Services\{{ class }}Service
    */
    protected $srv;

    /**
    * Constructor por defecto
    */
    function __construct() {
        $this->init();
        $this->setDomain('{{ class }}')
            ->setTemplatePath(__DIR__. DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'Templates');
    }

    /**
    * Index
    * @GET
    * @route /{{ url }}
    * @return string HTML
    */
    public function index() {
        return $this->render('index.html.twig');
    }
}
