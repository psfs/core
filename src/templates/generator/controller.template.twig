<?php
namespace {{ module }}\Controller;

use PSFS\base\types\{{ controllerType }}Controller;

/**
* Class {{ module }}Controller
* @package {{ module }}\Controller
* @author {{ get_config("author", "Fran L�pez") }} <{{ get_config("author_email", "fran.lopez84@hotmail.es") }}>
* @version {{ get_config("version", "1.0") }}
* @Path("{{ module }}")
* Autogenerated controller [{{ ""|date("Y-m-d H:i:s") }}]
*/
class {{ module }}Controller extends {{ controllerType }}Controller {
    const DOMAIN = '{{ module }}';

    /**
    * @Inyectable
    * @var \{{ module }}\Services\{{ module }}Service
    */
    protected $srv;

    /**
    * Constructor por defecto
    */
    function __construct() {
        parent::__construct();
        $this->setDomain('{{ module }}')
            ->setTemplatePath(__DIR__. DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'Templates');
    }

    /**
    * Index
    * @GET
    * @route /{{ module }}
    * @return string HTML
    */
    public function index() {
        return $this->render('index.html.twig');
    }
}
