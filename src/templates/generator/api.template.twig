<?php
namespace {{ namespace }}\Api{% if package %}{{ '\\' ~ package }}{% endif %};

use {{ namespace }}\Api{% if package %}{{ '\\' ~ package }}{% endif %}\base\{{ api }}BaseApi;
use {{ namespace }}\Models{% if package %}{{ '\\' ~ package }}{% endif %}\{{ api }}Query;
use Propel\Runtime\ActiveQuery\ModelCriteria;

/**
* Class {{ class }}
* @package {{ namespace }}\Api{% if package %}{{ '\\' ~ package }}{% else %}{{ "\n" }}{% endif %}
* @version {{ get_config("version", "1.0") }}
* @Api {{ api }}
*/
class {{ api }} extends {{ api }}BaseApi
{
    public function init() {
        parent::init();
        //$this->extraColumns = [];
    }

    public function joinTables(ModelCriteria &$query)
    {
        /** @var {{ api }}Query $query */
        //$query;
    }
}
