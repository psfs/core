{% set base_version = get_config('admin.version', 'v1') %}
{% extends base_version ~ '/base.html.twig' %}
{% block body_header %}
    <div class="page-header">
        <h1>{{ "No tienes privilegios para acceder"|trans }}</h1>
        <h2>{{ get_config('platform.name') }}</h2>
    </div>
{% endblock body_header %}
{% block body_content %}
    <p>{{ "La ruta a la que accedes %s es de acceso restringido"|trans|replace('%s', '<strong>' ~  route ~ '</strong>') }}</p>
{% endblock body_content %}
{% block scripts %}
{% endblock scripts %}
