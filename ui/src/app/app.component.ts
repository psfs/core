import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'psfs-root',
  template: '<main>PSFS UI POC · proxy HTTP activo</main>',
  changeDetection: ChangeDetectionStrategy.OnPush
})
export class AppComponent {}
