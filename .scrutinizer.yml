checks:
  php: true

build:
  tests:
    override:
      -
        command: 'phpunit --coverage-clover=psfs-coverage'
        coverage:
          file: 'psfs-coverage'
          format: 'clover'

filter:
  excluded_paths:
    - src/test/*
    - vendor/*
    - src/public/*
    - src/templates/*